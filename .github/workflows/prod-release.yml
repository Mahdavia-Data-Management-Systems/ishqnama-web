name: Prod Release

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  infra-prod:
    uses: ./.github/workflows/deploy-infra.yml
    with:
      environment: prod
    secrets: inherit
